#include <bits/stdc++.h>

#define endl '\n'

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    freopen("test.txt", "w", stdout);

    for (int i = 0; i < 96; i++) {
        cout << "gen " << i << " > $" << endl;
    }

    return 0;
}