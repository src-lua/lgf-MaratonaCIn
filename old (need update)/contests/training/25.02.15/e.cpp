#include <bits/stdc++.h>

#define endl '\n'

using namespace std;
using ll = long long;

int schema1(ll n) {
    (2*l -1 -k ==)
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    ll n; cin >> n;

    for (int i = 0; i < n; i++) {
        /* code */
    }
    

    return 0;
}