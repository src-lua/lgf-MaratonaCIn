/**
 * Author:      Lua
 * Problem:     I
 * Link:        https://codeforces.com/group/yc7Yxny414/contest/103049/problem/I
 * Status:      ~
 * Created:     12-07-2026 09:01:59
 **/

#include <bits/stdc++.h>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0); cout.tie(0);

    int n; cin >> n;
    vector<int> dist(n); for (auto &x : dist) cin >> x;
    vector<int> A(n), B(n), C(n);
    for (auto &x : A) cin >> x;
    for (auto &x : B) cin >> x;
    for (auto &x : C) cin >> x;

    


    return 0;
}
