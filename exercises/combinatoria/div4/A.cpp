#include <bits/stdc++.h>

#define endl '\n'

using namespace std;
using ll = long long;

ll getn(int n) {
    return ((1+n)*n)/2;
}


int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    int n, k, q;
    cin >> n >> k >> q;

    for (int i = 0; i < n; i++) {
        cin >> 
    }
    

    return 0;
}